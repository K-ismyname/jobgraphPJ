# LLM
openai>=1.0.0

# Agent framework — langgraph 1.x 필수 (0.x는 add_node에서 "노드명이 State
# TypedDict 필드명과 같으면 안 된다"는 검증이 있어, AppState.resume_eval 등과
# 이름이 같은 노드(supervisor.py의 add_node("resume_eval", ...))를 등록할 때
# ValueError로 그래프 빌드 자체가 실패한다 — 0.1.19~0.2.76 전 범위에서 재현 확인,
# 1.2.7부터 통과. langchain-core/langchain-openai도 1.x로 맞춰 세대 통일.
# bare `langchain` 패키지는 코드에서 직접 import하지 않아 불필요 — 제거.
langgraph>=1.0.0,<2
langchain-openai>=1.0,<2
langchain-core>=1.0,<2
# Graph DB
neo4j>=5.0.0

# API client
httpx>=0.27.0

# PDF
pdfplumber>=0.11.0
pymupdf>=1.24.0

# Data
pydantic>=2.0.0
python-dotenv>=1.0.0

# API serving
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Evaluation
ragas~=0.4.0
langfuse~=4.0

# Dev / test
pytest>=8.0.0
