# LLM
openai>=1.0.0

# Agent framework — 0.3.x/0.2.x 세대로 상한 고정 (코드가 이 API로 작성됨).
# 상한이 없으면 fresh install이 langgraph 1.x + langchain-core 1.x를 끌어와
# 부분 업그레이드 시 core/langchain 버전 불일치(langchain.debug AttributeError)가 발생.
langgraph>=0.2.28,<0.3
langchain>=0.3.30,<0.4
langchain-openai>=0.2,<0.4
langchain-core>=0.3.86,<0.4
# Graph DB
neo4j>=5.0.0

# API client
httpx>=0.27.0

# PDF
pdfplumber>=0.11.0
pymupdf>=1.24.0

# Data
pydantic>=2.0.0
python-dotenv>=1.0.0

# API serving
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Evaluation
ragas~=0.4.0
langfuse~=4.0

# Dev / test
pytest>=8.0.0
