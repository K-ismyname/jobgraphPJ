# Web UI (이력서 분석 데모) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** FastAPI가 same-origin으로 서빙하는 정적 HTML+JS 한 장으로, 이력서 PDF 업로드 → 직군 선택 → 분석 → 적합도·신뢰도·검증·코칭 결과를 보여주는 포트폴리오 데모를 만든다.

**Architecture:** 프론트는 `web/`의 정적 파일 3개(`index.html`/`style.css`/`app.js`, 빌드 도구 없는 바닐라). FastAPI가 `GET /`로 `index.html`을, `/web/*`로 자산을 서빙한다(same-origin → CORS 불필요). 기존 v3 API(`/portfolio/upload`·`/analyze`·`/report/{id}`)를 그대로 호출하며 백엔드 신규 로직은 없다.

**Tech Stack:** FastAPI `StaticFiles`/`FileResponse`, 바닐라 JS(`fetch`), CSS. 외부 JS 라이브러리 없음.

**참고 — 소비하는 API 계약(기존):**
- `POST /portfolio/upload` (multipart `file`, .pdf만) → `{report_id, candidate_name_hint, page_count, text_length, status:"uploaded"}`
- `POST /portfolio/analyze` (JSON `{report_id, job_family, owner_name?, github_url?, deploy_url?}`) → `{report_id, status:"processing"}`
- `GET /portfolio/report/{report_id}` → `{report_id, status:"processing"|"done"|"error", owner, job_family, match_rate, confidence_level, advice, verification_counts, verified_skills:[{skill,verification,sources}], coaching_summary, suggestions:[{target_section,missing_skill,original_text,rewritten_text,expected_impact,priority,verified}], error_detail, generated_at}`

**직군 10개(드롭다운 하드코딩):** Software Engineer, Data Engineer, Data Analyst, Data Scientist, AI/LLM Engineer, ML Engineer, DevOps/SRE, Security Engineer, Frontend Engineer, Architect.

---

### Task 1: FastAPI 정적 서빙 배선

**Files:**
- Create: `web/index.html` (최소 골격 — Task 2에서 확장)
- Modify: `src/api/main.py` (StaticFiles 마운트 + `GET /`)
- Test: `tests/unit/test_static_serving.py`

- [ ] **Step 1: 최소 index.html 생성** (StaticFiles 마운트가 import 시점에 디렉토리를 요구하므로 먼저 만든다)

`web/index.html`:
```html
<!DOCTYPE html>
<html lang="ko">
<head><meta charset="utf-8"><title>Job Skill Analyzer</title></head>
<body><h1>Job Skill Analyzer</h1></body>
</html>
```

- [ ] **Step 2: 실패하는 테스트 작성**

`tests/unit/test_static_serving.py`:
```python
# 정적 프론트 서빙 라우트 스모크 — lifespan 없이 라우트만 검증(Neo4j 키 불필요)
from fastapi.testclient import TestClient

from src.api.main import app


def test_index_served():
    # with 없이 TestClient를 쓰면 lifespan(=Neo4j 연결)을 돌리지 않는다
    client = TestClient(app)
    r = client.get("/")
    assert r.status_code == 200
    assert "text/html" in r.headers["content-type"]
    assert "Job Skill Analyzer" in r.text
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `python -m pytest tests/unit/test_static_serving.py -v`
Expected: FAIL — `GET /`가 404(아직 라우트 없음).

- [ ] **Step 4: main.py에 정적 서빙 추가**

`src/api/main.py`의 import에 추가:
```python
from fastapi.responses import FileResponse, JSONResponse
from fastapi.staticfiles import StaticFiles
```
(기존 `from fastapi.responses import JSONResponse`를 위 한 줄로 대체)

`app.include_router(...portfolio...)` 두 줄 **아래**에 추가:
```python
app.mount("/web", StaticFiles(directory="web"), name="web")


@app.get("/", include_in_schema=False)
async def index() -> FileResponse:
    """프론트 진입점 — 정적 index.html 반환."""
    return FileResponse("web/index.html")
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `python -m pytest tests/unit/test_static_serving.py -v`
Expected: PASS

- [ ] **Step 6: 전체 단위 테스트로 회귀 확인 후 커밋**

Run: `python -m pytest tests/unit/ -q`
Expected: 기존 통과 수 + 1 (모두 PASS)
```bash
git add web/index.html src/api/main.py tests/unit/test_static_serving.py
git commit -m "feat(api): 정적 프론트 서빙 배선 — GET / + /web StaticFiles"
```

---

### Task 2: index.html 마크업 + style.css

**Files:**
- Modify: `web/index.html` (3개 섹션 전체 마크업)
- Create: `web/style.css`

UI 자동 테스트는 비용 대비 가치가 낮아(정적 파일) 수동 확인한다. 라우트 스모크는 Task 1·4가 커버한다.

- [ ] **Step 1: index.html 전체 마크업 작성**

`web/index.html`:
```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Job Skill Analyzer — 이력서 분석</title>
  <link rel="stylesheet" href="/web/style.css">
</head>
<body>
  <main>
    <header>
      <h1>Job Skill Analyzer</h1>
      <p class="sub">이력서를 올리면 직무 <b>적합도</b>와 그 판단의 <b>신뢰도</b>를 분리해 알려줍니다.</p>
    </header>

    <!-- 1단계: 업로드 -->
    <section id="step-upload" class="card">
      <h2>1. 이력서 업로드</h2>
      <input type="file" id="file" accept="application/pdf">
      <button id="upload-btn">업로드</button>
      <p id="upload-msg" class="msg"></p>
    </section>

    <!-- 2단계: 분석 입력 -->
    <section id="step-analyze" class="card disabled">
      <h2>2. 분석 조건</h2>
      <label>직군
        <select id="job-family">
          <option>Software Engineer</option>
          <option>Data Engineer</option>
          <option>Data Analyst</option>
          <option>Data Scientist</option>
          <option selected>AI/LLM Engineer</option>
          <option>ML Engineer</option>
          <option>DevOps/SRE</option>
          <option>Security Engineer</option>
          <option>Frontend Engineer</option>
          <option>Architect</option>
        </select>
      </label>
      <label>GitHub URL (선택)
        <input type="url" id="github-url" placeholder="https://github.com/owner/repo">
      </label>
      <label>배포 URL (선택)
        <input type="url" id="deploy-url" placeholder="https://my-service.example.com">
      </label>
      <button id="analyze-btn">분석 시작</button>
      <p id="analyze-msg" class="msg"></p>
    </section>

    <!-- 3단계: 결과 -->
    <section id="step-result" class="card disabled">
      <h2>3. 결과</h2>
      <div id="progress" class="hidden"><span class="spinner"></span> 분석 중…</div>
      <div id="result"></div>
    </section>
  </main>
  <script src="/web/app.js"></script>
</body>
</html>
```

- [ ] **Step 2: style.css 작성**

`web/style.css`:
```css
:root { --fg:#1a1a2e; --muted:#6b7280; --line:#e5e7eb; --accent:#4f46e5;
  --verified:#16a34a; --corroborated:#d97706; --claimed:#9ca3af; }
* { box-sizing: border-box; }
body { margin:0; font-family: system-ui, -apple-system, sans-serif; color:var(--fg);
  background:#f7f7fb; line-height:1.5; }
main { max-width: 720px; margin: 0 auto; padding: 32px 20px 80px; }
header h1 { margin:0 0 4px; }
.sub { color:var(--muted); margin:0 0 24px; }
.card { background:#fff; border:1px solid var(--line); border-radius:12px;
  padding:20px; margin-bottom:16px; }
.card.disabled { opacity:.45; pointer-events:none; }
.card h2 { margin:0 0 12px; font-size:1.05rem; }
label { display:block; margin:10px 0; font-size:.9rem; color:var(--muted); }
select, input[type=url] { display:block; width:100%; margin-top:4px; padding:8px;
  border:1px solid var(--line); border-radius:8px; font-size:.95rem; }
button { margin-top:12px; padding:9px 18px; border:0; border-radius:8px;
  background:var(--accent); color:#fff; font-size:.95rem; cursor:pointer; }
button:disabled { background:#c7c7d1; cursor:not-allowed; }
.msg { font-size:.85rem; margin:8px 0 0; }
.msg.error { color:#dc2626; }
.hidden { display:none; }
.spinner { display:inline-block; width:14px; height:14px; border:2px solid var(--line);
  border-top-color:var(--accent); border-radius:50%; animation:spin .8s linear infinite;
  vertical-align:middle; }
@keyframes spin { to { transform: rotate(360deg); } }
/* 결과 */
.metrics { display:flex; gap:16px; margin-bottom:16px; }
.metric { flex:1; border:1px solid var(--line); border-radius:10px; padding:14px; }
.metric .big { font-size:1.6rem; font-weight:700; }
.gauge { height:8px; background:var(--line); border-radius:4px; overflow:hidden; margin-top:6px; }
.gauge > span { display:block; height:100%; background:var(--accent); }
.badge { display:inline-block; padding:2px 8px; border-radius:999px; font-size:.75rem;
  color:#fff; }
.badge.Verified { background:var(--verified); }
.badge.Corroborated { background:var(--corroborated); }
.badge.Claimed { background:var(--claimed); }
.skill-row { display:flex; align-items:center; gap:8px; padding:6px 0;
  border-bottom:1px solid var(--line); font-size:.9rem; }
.skill-row .src { color:var(--muted); font-size:.78rem; margin-left:auto; }
.suggestion { border:1px solid var(--line); border-radius:10px; padding:12px; margin-top:10px; }
.suggestion .head { font-weight:600; font-size:.9rem; }
.suggestion .rew { background:#f3f4f6; border-radius:6px; padding:8px; margin:6px 0;
  font-size:.85rem; white-space:pre-wrap; }
.prio { font-size:.72rem; color:var(--muted); }
h3 { margin:18px 0 6px; font-size:.95rem; }
```

- [ ] **Step 3: 수동 확인 + 커밋**

Run: `uvicorn src.api.main:app --port 8000` 후 브라우저에서 `http://localhost:8000/` 열기.
Expected: 3개 카드(업로드 활성, 분석·결과는 흐릿하게 비활성)가 중앙 컬럼에 보인다.
```bash
git add web/index.html web/style.css
git commit -m "feat(web): 이력서 분석 UI 마크업·스타일"
```

---

### Task 3: app.js — 업로드 → 분석 → 폴링 → 렌더

**Files:**
- Create: `web/app.js`

전역 상태는 `reportId` 하나. 함수 4개로 분리한다. 자동 테스트 대신 실제 PDF로 흐름을 수동 확인한다.

- [ ] **Step 1: app.js 작성**

`web/app.js`:
```javascript
// 이력서 분석 데모 — 업로드→분석→폴링→결과 렌더 (바닐라 JS)
const state = { reportId: null };

const $ = (id) => document.getElementById(id);

function setMsg(el, text, isError = false) {
  el.textContent = text;
  el.classList.toggle("error", isError);
}

// 1. 업로드
async function uploadResume() {
  const file = $("file").files[0];
  if (!file) return setMsg($("upload-msg"), "PDF 파일을 선택하세요.", true);
  const fd = new FormData();
  fd.append("file", file);
  setMsg($("upload-msg"), "업로드 중…");
  try {
    const res = await fetch("/portfolio/upload", { method: "POST", body: fd });
    if (!res.ok) {
      const e = await res.json().catch(() => ({}));
      return setMsg($("upload-msg"), `업로드 실패: ${e.detail || res.status}`, true);
    }
    const data = await res.json();
    state.reportId = data.report_id;
    setMsg($("upload-msg"), `업로드됨: ${data.candidate_name_hint} (${data.page_count}쪽)`);
    $("step-analyze").classList.remove("disabled");
  } catch (err) {
    setMsg($("upload-msg"), `네트워크 오류: ${err.message}`, true);
  }
}

// 2. 분석 시작
async function startAnalysis() {
  if (!state.reportId) return;
  const body = {
    report_id: state.reportId,
    job_family: $("job-family").value,
    github_url: $("github-url").value || null,
    deploy_url: $("deploy-url").value || null,
  };
  setMsg($("analyze-msg"), "");
  try {
    const res = await fetch("/portfolio/analyze", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    if (!res.ok) {
      const e = await res.json().catch(() => ({}));
      return setMsg($("analyze-msg"), `분석 시작 실패: ${e.detail || res.status}`, true);
    }
    $("step-result").classList.remove("disabled");
    $("progress").classList.remove("hidden");
    $("result").innerHTML = "";
    pollReport(0);
  } catch (err) {
    setMsg($("analyze-msg"), `네트워크 오류: ${err.message}`, true);
  }
}

// 3. 폴링 (3초 간격, 최대 100회 = 5분)
async function pollReport(attempt) {
  if (attempt > 100) {
    $("progress").classList.add("hidden");
    $("result").innerHTML = "<p class='msg error'>분석이 지연됩니다. 잠시 후 다시 시도하세요.</p>";
    return;
  }
  try {
    const res = await fetch(`/portfolio/report/${state.reportId}`);
    const data = await res.json();
    if (data.status === "processing") {
      setTimeout(() => pollReport(attempt + 1), 3000);
      return;
    }
    $("progress").classList.add("hidden");
    if (data.status === "error") {
      $("result").innerHTML = `<p class='msg error'>분석 오류: ${data.error_detail || "알 수 없음"}</p>`;
      return;
    }
    renderReport(data);
  } catch (err) {
    setTimeout(() => pollReport(attempt + 1), 3000);
  }
}

// 4. 결과 렌더
function renderReport(d) {
  const pct = d.match_rate <= 1 ? Math.round(d.match_rate * 100) : Math.round(d.match_rate);
  const counts = d.verification_counts || {};
  const skills = (d.verified_skills || [])
    .map((s) => `<div class="skill-row"><span>${s.skill}</span>
      <span class="badge ${s.verification}">${s.verification}</span>
      <span class="src">${(s.sources || []).join(", ")}</span></div>`)
    .join("");
  const suggestions = (d.suggestions || [])
    .map((s) => `<div class="suggestion">
      <div class="head">${s.missing_skill} → ${s.target_section}
        <span class="prio">[${s.priority}${s.verified ? " · 검증됨" : ""}]</span></div>
      <div class="rew">${s.rewritten_text}</div>
      <div class="prio">기대효과: ${s.expected_impact}</div></div>`)
    .join("");

  $("result").innerHTML = `
    <div class="metrics">
      <div class="metric"><div>적합도</div><div class="big">${pct}%</div>
        <div class="gauge"><span style="width:${pct}%"></span></div></div>
      <div class="metric"><div>신뢰도</div><div class="big">${d.confidence_level || "-"}</div>
        <div class="prio">Verified ${counts.Verified || 0} · Corroborated ${counts.Corroborated || 0} · Claimed ${counts.Claimed || 0}</div></div>
    </div>
    ${d.advice ? `<p>${d.advice}</p>` : ""}
    <h3>검증된 스킬</h3>${skills || "<p class='prio'>없음</p>"}
    <h3>코칭${d.coaching_summary ? "" : ""}</h3>
    ${d.coaching_summary ? `<p>${d.coaching_summary}</p>` : ""}
    ${suggestions || "<p class='prio'>제안 없음</p>"}
  `;
}

$("upload-btn").addEventListener("click", uploadResume);
$("analyze-btn").addEventListener("click", startAnalysis);
```

- [ ] **Step 2: 실제 PDF로 수동 흐름 확인**

Run: `uvicorn src.api.main:app --port 8000` (`.env`에 OPENAI/NEO4J 키 필요), 브라우저에서 PDF 업로드 → 직군 선택 → 분석 시작.
Expected: 업로드 메시지 표시 → 2단계 활성 → 분석 시작 시 스피너 → 수십 초 후 적합도 %·신뢰도 배지·검증 스킬·코칭이 렌더된다.

- [ ] **Step 3: 커밋**
```bash
git add web/app.js
git commit -m "feat(web): 업로드·분석·폴링·결과 렌더 JS"
```

---

### Task 4: /web 자산 서빙 테스트 보강 + 최종 통합

**Files:**
- Modify: `tests/unit/test_static_serving.py`

- [ ] **Step 1: 자산 서빙 테스트 추가**

`tests/unit/test_static_serving.py`에 함수 추가:
```python
def test_static_assets_served():
    client = TestClient(app)
    for path, ctype in [("/web/app.js", "javascript"), ("/web/style.css", "css")]:
        r = client.get(path)
        assert r.status_code == 200, path
        assert ctype in r.headers["content-type"], path
```

- [ ] **Step 2: 테스트 통과 확인**

Run: `python -m pytest tests/unit/test_static_serving.py -v`
Expected: 2개 함수 모두 PASS (app.js·style.css가 Task 2·3에서 생성됨).

- [ ] **Step 3: 전체 단위 테스트 + 커밋**

Run: `python -m pytest tests/unit/ -q`
Expected: 모두 PASS
```bash
git add tests/unit/test_static_serving.py
git commit -m "test(web): 정적 자산 서빙 스모크 보강"
```

- [ ] **Step 4: Docker 재빌드로 컨테이너 포함 확인(선택)**

`COPY . .`가 `web/`를 이미지에 포함하므로 별도 Dockerfile 수정 불필요. `.dockerignore`에 `web` 없음을 확인:
Run: `grep -w web .dockerignore || echo "web 포함됨 OK"`
Expected: `web 포함됨 OK`

---

## 비결정 사항(구현 중 확정)

- `match_rate` 스케일: `<= 1`이면 ×100, 아니면 그대로(코드에 반영됨).
- 직군 목록은 하드코딩(MVP, API 의존 최소화).
