# JobGraph — 프로젝트 페이지 제작 브리핑

> 이 문서는 프로젝트 소개 페이지(포트폴리오/랜딩)를 만들기 위한 콘텐츠 브리핑입니다.
> 각 섹션은 페이지의 블록으로 바로 옮길 수 있게 구성했습니다.
> 문서 맨 끝에 디자이너를 위한 톤·강조점 가이드가 있습니다.

---

## 0. 메타 정보

- **프로젝트명:** JobGraph
- **한 줄 정의:** 채용공고를 분석하고, 이력서·GitHub·포트폴리오·배포 URL을 종합해 직무 **적합도**와 그 판단의 **신뢰도**를 분리해서 알려주는 Agentic RAG 시스템
- **타겟 사용자:** AI/LLM 애플리케이션 엔지니어를 포함한 개발 직군 구직자
- **라이브 데모:** https://gaheelee-job-graph.hf.space
- **관측 페이지:** https://gaheelee-job-graph.hf.space/observe
- **소스 코드:** https://github.com/K-ismyname/jobgraphPJ
- **프로젝트 성격:** 개인 프로젝트 / 실서비스 배포 완료

---

## 1. 히어로 (페이지 최상단)

**태그라인 후보:**
- "당신의 실력을, 이름이 달라서 없다고 말하지 않는 커리어 분석기"
- "적합도와 신뢰도를 분리한다 — '할 줄 안다는 주장'과 '코드로 증명된 실력'은 다르다"
- "왜 이 답을 신뢰할 수 있는가에 코드로 답하는 Agentic RAG"

**한 문장 요약:**
이력서 한 장으로 "이 사람이 이 직무에 맞나"를 판단하면 두 개의 다른 질문이 뒤섞여 망가진다. JobGraph는 **직무가 요구하는 스킬을 가졌는가(적합도)**와 **그 스킬이 실제로 검증됐는가(신뢰도)**를 분리해서, 여러 독립 소스의 교차검증으로 신뢰도를 *결정적으로* 산출한다.

**CTA:** "지금 분석해보기" → 라이브 데모

---

## 2. 무엇을 푸는가 (Problem 섹션)

기존 이력서 분석·매칭 서비스의 근본 문제:

1. **주장과 실증을 같은 무게로 다룬다.** 이력서에 "Python 능숙"이라고 적으면 그대로 믿는다. 그러면 "다 잘하는 사람"처럼 부풀거나, 반대로 축소된다.
2. **LLM이 판단을 지어낸다.** "매칭률 82%" 같은 숫자를 LLM이 만들면 근거가 없다. 왜 82%인지 아무도 설명 못 한다.
3. **이름 매칭의 한계.** "LangGraph로 멀티에이전트를 만든 사람"에게 공고가 "LLM 경험"을 요구하면, 단어가 안 겹친다는 이유로 "LLM 경험 부족"이라고 말한다.

JobGraph는 이 세 가지를 **아키텍처로** 해결한다.

---

## 3. 핵심 가치 — 3개 축 (Feature 섹션, 페이지의 중심)

### ① 적합도 ⊥ 신뢰도, 두 축 분리

최종 결과는 하나의 점수가 아니라 **직교하는 두 축**으로 나온다.

| 축 | 무엇을 답하나 | 산출 방식 |
|----|--------------|----------|
| **적합도** | 직군 핵심 스킬 중 몇 개를 가졌나 | 직군 빈도 상위 10개 스킬 ∩ 보유 스킬 (코드 계산) |
| **신뢰도** | 그 보유가 얼마나 검증됐나 | 합의 노드가 검증 등급을 결정적으로 판정 |

두 축이 직교하므로 "스킬은 맞는데 근거가 약함"과 "근거는 확실한데 스킬이 부족함"을 구별할 수 있다. 적합도는 `6/10 충족`처럼 **분수**로 표기해 거친 측정을 정밀 점수로 오해하지 않게 한다.

### ② 증거 강도 기반 검증 등급

검증 등급은 **어느 소스냐가 아니라 증거가 얼마나 강하냐**로 정한다.

```
Verified     코드 근거(의존성 파일·주 언어)로 실증됨
Corroborated 2개 이상 독립 소스가 일치 (코드 근거는 없지만 서로 뒷받침)
Claimed      1개 소스만 (코드·실증 미확인 — 주장/언급)
```

핵심: **README나 배포 페이지에 스킬명이 적혀 있기만 한 것은 Verified가 아니다.** 실제 코드·의존성 근거가 있어야 한다. "이력서 주장 vs 코드 실증"이라는 두 축 분리를 실제로 지키는 판정 로직.

**예시 출력:**
```
React    Verified     [github, deploy, resume]   ← 코드+작동+주장 모두 일치
Python   Corroborated [portfolio, resume]        ← 두 소스 일치
Docker   Claimed      [resume]                   ← 이력서 주장만
```

### ③ 간접 실증 — 이름이 달라서 생기는 거짓 미보유 방지

"LangGraph로 멀티에이전트 RAG를 만들었는데 'LLM 경험 부족'"이라는 오탐을 막는다. 지식 그래프에 `LangGraph → LangChain → LLM → GenAI → AI` 같은 생태계 관계(PART_OF)를 두고, **검증된 구체 스킬이 있으면 상위 카테고리 스킬을 간접 실증으로 승격**한다. 단 이력서 주장(Claimed)만으로는 승격하지 않는다 — 실증된 스킬만 근거가 된다.

---

## 4. 어떻게 동작하나 (How it works — 다이어그램 섹션)

입력에 있는 소스만 병렬로 평가한 뒤, 결정적 합의 → 갭 분석 → 검증 → 코칭으로 흐른다.

```
[입력] 이력서 PDF · GitHub URL · 포트폴리오 PDF · 배포 URL (모두 선택)
   │
   ▼  입력에 있는 소스만 병렬 fan-out
┌──────────┬──────────┬──────────┬──────────┐
│ 이력서    │ GitHub    │ 포트폴리오 │ 배포 URL  │   ← 서로 다른 modality라
│ 텍스트    │ 코드/의존성│ 텍스트+이미지│ 웹 HTML  │     한 LLM에 못 합침
└────┬─────┴────┬─────┴────┬─────┴────┬─────┘
     └──────────┴──────────┴──────────┘
                    │
                    ▼
         합의 노드 (검증 등급 결정적 판정 + 간접 실증)   ← LLM 미사용
                    ▼
         Gap 에이전트 (Corrective RAG 루프)              ← 증거 부족하면 스스로 재검색
                    ▼
         Synthesizer (적합도·신뢰도 리포트 + 결정적 수치 덮어쓰기)
                    ▼
         Critic (합의와 대조해 환각 제거·라벨 교정)       ← LLM 미사용
                    ▼
         Coach 에이전트 (코칭 생성 + 파일 경로 환각 차단)
                    ▼
              [결과] 5개 블록 리포트
```

**멀티에이전트인 이유:** 4개 소스는 각각 텍스트·코드·이미지·웹이라는 서로 다른 형식이라 하나의 LLM 호출로 합칠 수 없다. "억지 멀티에이전트"가 아니라 **기능이 요구하는 멀티에이전트**. 자율적으로 판단·반복하는 진짜 에이전트는 Gap과 Coach 둘 — 근거가 부족하면 스스로 도구를 더 부르는 Corrective RAG 루프.

---

## 5. 결과 화면 — 5개 블록 (Screenshot/UI 섹션)

점수 게이지 대신 **행동 가능한 5개 구조**로 제시한다.

1. **충족한 스킬** — 직군 핵심 스킬 중 보유한 것 (검증 등급 배지: Verified/Corroborated/Claimed)
2. **채우면 좋을 스킬** — 없는 직군 스킬 + 왜 필요한지(실제 공고 발췌·요구 건수 인용) + 어떻게 학습할지
3. **코드로 보강할 스킬** — GitHub 코드를 실제로 읽고, 실존하는 파일·패턴에 근거한 파일 레벨 제안
4. **지원해볼 만한 회사** — 검증된 스킬로 매칭한 실제 공고 + 지원 링크
5. **관측 페이지** — 분석이 실제로 어떤 경로로 실행됐는지 다이어그램 + 단계별 데이터 흐름

---

## 6. 핵심 설계 원칙 — "왜 이렇게 만들었나" (Deep-dive 섹션)

이 프로젝트의 차별점은 전부 하나의 원칙에서 나온다: **"사실은 코드가 공급·검증하고, LLM은 문장만 다듬는다."**

### 원칙 1 — 신뢰해야 할 값은 LLM이 아니라 코드가 계산한다
- 검증 등급, 매칭률, 신뢰도 수치, 코칭의 근거(왜 이 스킬이 필요한가)를 전부 **결정적 코드**로 산출하고 LLM 출력을 덮어쓴다.
- 코칭이 언급하는 파일 경로도 실제 레포와 대조해 없는 파일이면 그 제안 자체를 제거한다.
- 결과: "왜 이 답을 신뢰할 수 있나"에 코드로 답할 수 있다.

### 원칙 2 — 효과가 측정된 것만 남긴다
- **벡터 DB 제거:** Chroma 벡터 검색이 실제로 기여하는지 측정하니, 검색 12건 중 11건이 단순 키워드 매칭과 동일했다. → 제거하고 그래프 질의로 통합. "있으면 좋아 보여서"가 아니라 효과로 판단.
- **역량 → 스킬 전환:** 스킬을 "역량"으로 묶자 Data Analyst와 Data Engineer가 동일하게 평가됐다(두 직군 핵심 스킬 유사도 0.14인데 역량으로 묶으면 구분 소실). → 개별 스킬 비교로 전환해 직군 변별력 확보.
- **임계값 튜닝:** 공통 스킬 판정 임계값을 5에서 7로 올려 ML/AI 데이터 편향 스킬 제거 (측정으로 결정).

### 원칙 3 — 멀티에이전트는 억지가 아니라 기능에서 발생한다
- 4개 소스가 서로 다른 modality라 나눌 수밖에 없었다.
- 자율 에이전트는 Corrective RAG가 실제로 필요한 Gap·Coach 둘뿐. 나머지는 결정적 코드.

---

## 7. 기술 스택 (Tech 섹션)

| 역할 | 기술 | 선택 이유 |
|------|------|----------|
| 에이전트 오케스트레이션 | **LangGraph** (StateGraph, Send, 서브그래프) | 조건 분기·병렬 fan-out·루프를 동시에 구현 |
| LLM | **OpenAI** gpt-4o-mini(기본)·gpt-4o(코드 심층분석)·vision | 비용 계층화, 단일 공급자 |
| 그래프 DB | **Neo4j Aura** | 직무–스킬 관계, 생태계(PART_OF)·동시등장(CO_OCCURS) 표현 |
| 데이터 소스 | Adzuna / The Muse API | 채용공고 수집 |
| PDF 처리 | pdfplumber(텍스트) + PyMuPDF(이미지 렌더) | 멀티모달 |
| 평가 | **RAGAS**(faithfulness) + **Langfuse**(트레이싱) | RAG 품질 측정 + 실행 추적 |
| 서빙 | **FastAPI + Docker** | 정적 웹 프론트 동시 서빙 |
| 배포 | **Hugging Face Spaces** (Docker) | 무료·라이브 URL |

**규모:** 소스 코드 약 6,000줄, 단위 테스트 241개.

---

## 8. 엔지니어링 하이라이트 (개발 역량 어필용)

이 프로젝트에서 실제로 잡은 문제들 — 신뢰성을 지키기 위한 결정적 방어:

- **재실행 데이터 오염 방지:** 파이프라인을 다시 돌려도 카운터가 이중 집계되지 않는 멱등 적재.
- **검증 등급 재설계:** 소스 기반 → 증거 강도(코드 vs 언급) 기반으로 판정 로직 재구성.
- **환각 4중 방어:** 실제 파일 목록 대조, code_anchor 플래그, CO_OCCURS 방향 제약, 결정적 라벨 덮어쓰기.
- **거짓 음성 방어:** "가진 스킬을 없다고 하는" 오탐을 PART_OF 간접 실증으로 제거.
- **API 견고성:** GitHub API의 rate limit(403) 응답을 정상 데이터로 오인하던 근본 버그 수정, async 블로킹 해소, XSS·리소스 누수 차단.

---

## 9. 정직한 한계 (신뢰를 위해 명시 — Limitations 섹션)

과장하지 않는 게 이 프로젝트의 정체성이므로 한계도 드러낸다.

- **배포 평가자:** 프론트·작동은 보이지만 백엔드·AI 기술은 외부에서 안 보인다. 주 가치는 "작동 실증", GitHub 코드와 교차할 때 강한 검증.
- **소규모 직군 데이터:** 공고 수가 적은 직군은 핵심 스킬 풀이 빈약할 수 있다. 데이터가 쌓일수록 정확해진다.
- **코칭 서술의 한계:** 학습 추천의 "왜"는 그래프 사실로 조립하지만, "어떻게"(자유 서술)는 다른 항목이 파악한 사실과 가끔 어긋날 수 있다. 구조화된 값은 결정적으로 막을 수 있어도 자유 서술문의 내부 일관성은 근본적으로 다른 문제라, 이 경계에서 전략적으로 멈췄다.

---

## 10. 디자이너를 위한 톤·강조점 가이드

**핵심 인상:** "성실하고 정직한 엔지니어링." 화려함보다 **신뢰·근거·측정**이 이 프로젝트의 정체성이다.

**강조해야 할 것 (우선순위):**
1. **적합도 ⊥ 신뢰도 두 축 분리** — 이 프로젝트를 한 장으로 설명하는 핵심. 두 축이 직교하는 시각적 표현(2x2, 또는 나란한 두 게이지)이 좋다.
2. **검증 등급 배지** (Verified / Corroborated / Claimed) — 색으로 강도를 표현. Verified=강(초록/파랑), Corroborated=중(노랑), Claimed=약(회색). 이 배지가 페이지 전반의 시각 언어가 될 수 있다.
3. **"LLM이 지어내는 걸 코드가 막는다"** — before/after 대비로 보여주면 강하다 (일반론 vs 실제 공고 발췌 인용).
4. **측정 기반 의사결정** — "12건 중 11건 동일 → 벡터 DB 제거" 같은 수치를 그대로 노출.

**피해야 할 것:**
- 과장된 "AI가 다 해결" 톤 — 이 프로젝트는 정확히 그 반대(LLM을 불신하고 코드로 검증)를 판다.
- 정밀 점수 게이지(82.4%) — 의도적으로 분수(6/10)를 쓰는 프로젝트라 정밀 점수 UI는 철학과 충돌.

**추천 색/무드:** 신뢰를 상징하는 차분한 블루/인디고 계열(현재 데모도 blue→indigo 그라데이션). 데이터·그래프 느낌.

**페이지 흐름 제안:**
히어로(태그라인 + 데모 CTA) → 문제 → 3개 핵심 축 → 동작 다이어그램 → 결과 화면 5블록 → 설계 원칙 3가지 → 기술 스택 → 엔지니어링 하이라이트 → 정직한 한계 → 푸터(데모·GitHub 링크)
